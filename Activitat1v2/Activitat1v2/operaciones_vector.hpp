#ifndef OPERACIONES_VECTOR_HPP
#define OPERACIONES_VECTOR_HPP

#include <vector>

int SumaImpares(const std::vector<int> &v);
int ProductoPares(const std::vector<int> &v);
int Divisibles3(const std::vector<int> &v, std::vector<int> &vResultado);
void ImprimirVector(const std::vector<int> &v);

#endif